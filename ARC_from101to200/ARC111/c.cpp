#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    int n;
    cin >> n;
    vector<int> s(n), p(n);
    for (int i; i < n;i++){
        cin >> p[i];
        s[i] = i + 1;
    }
}