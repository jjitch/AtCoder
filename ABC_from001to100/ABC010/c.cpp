#include <bits/stdc++.h>
#include <math.h>
using namespace std;
using ll = long long;


int main()
{
    int sx, sy, ex, ey, t, v, n;
    cin >> sx >> sy >> ex >> ey >> t >> v;
    cin >> n;
    for (int i = 0; i < n; i++)
    {
        int x, y;
        cin >> x >> y;
        double a, b;
    }
}